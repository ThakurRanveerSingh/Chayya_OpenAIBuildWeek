# Local API port.
PORT=3001

# Sensitive values detected in a browser recording are replaced with placeholders,
# for example ANUKRITI_SECRET_1. Set those only in the secure execution runtime;
# never commit them to source control.
# ANUKRITI_SECRET_1=replace-me
